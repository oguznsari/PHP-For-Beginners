<?php


$business = [
    'name' => 'Laracasts',
    'cost' => 15,
    'categories' => ['Testing', 'PHP', 'JavaScript']
];

function register($user) {
    // Create the user record in the db.
    // Log them in.
    // Sende a welcome email.
    // Redirect to their dashboard.
}

require 'index.view.php';